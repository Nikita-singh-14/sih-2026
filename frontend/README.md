# MeasureSure Frontend

React and Vite frontend for the Legal Metrology verification platform.

## Run locally

```powershell
npm install
npm run dev
```

Open `http://localhost:5173`.

The Vite development server proxies `/api` requests to the Express backend at `http://localhost:3000`.

## Build

```powershell
npm run build
npm run lint
```
